# API base URL (no trailing slash). See mobile-app/API-AND-DATA.md.
# EXPO_PUBLIC_API_BASE_URL=https://your-api-host.com
#
# If the feed fails to load: use the exact API origin (e.g. https://api.desynar.com
# if the API lives there). Use https:// in production. Restart Expo after changing.
