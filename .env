# API base URL (no trailing slash). See mobile-app/API-AND-DATA.md.
# Use https to avoid HTTP→HTTPS redirect; RN fetch can turn POST into GET on redirect.
EXPO_PUBLIC_API_BASE_URL=https://api.desynar.com